---
import DefaultLayout from './DefaultLayout.astro'

const { frontmatter, slug } = Astro.props
---

<DefaultLayout title={frontmatter.title} url={slug}>
  <div class="container">
    <div class="space-content my-12">
      <slot />
    </div>
  </div>
</DefaultLayout>
